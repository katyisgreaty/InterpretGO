<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>InterpretGO</title>
</head>
<body>
    <nav class="navbar navbar-fixed-top" role="navigation">
        <div class="container navlinks">
            <div class="navbar-header">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand fix" href="/">InterpretGO</a>
            </div>
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li>
                        @Html.ActionLink("Events and Requests", "Index", "Assignments")
                    </li>
                    <li>
                        @Html.ActionLink("Interpreters", "Index", "Interpreters")
                    </li>
                    <li>
                        @Html.ActionLink("Deaf Users", "Index", "Clients")
                    </li>
                    @if (User.Identity.IsAuthenticated)
                    {
                        <li>
                            @Html.ActionLink("Worker Dashboard", "Index", "Workers")
                        </li>
                    }

                    @if (User.Identity.IsAuthenticated)
                    {
                        <li>
                            @Html.ActionLink("Log Out", "LogOff", "Account")
                        </li>
                    }

                </ul>
            </div>
        </div>
    </nav>
    <div>
        @RenderBody()
    </div>
</body>
</html>
